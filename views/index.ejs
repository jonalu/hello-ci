<!doctype html>
<html>
<head>
  <script src="/socket.io/socket.io.js"></script>
</head>
<body>
  <h1>Socket.IO app</h1>
  <script>
    var socket = io();

    function startPinging () {
      socket.emit('pingo', 'my emit msg')
      setTimeout(function(){
        startPinging()
      }, 5000)
    }

    socket.on('schedule', function(schedule) {
      console.log('schedule recvd')
    })

    socket.on('events', function(events) {
      console.log('events recvd', events)
    })

    startPinging()
  </script>
</body>
</html>
